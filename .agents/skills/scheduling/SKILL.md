---
name: scheduling
description: 排班模块：周模板整体替换（PUT）、日期例外（请假 off / 自定义 custom）、求值优先级，以及最重要的「主数据变更与既有预约的冲突保护」（409 + 受影响清单 + force）。写排班接口/页面、处理请假与改班次时加载。
whenToUse: 实现或修改 weekly-shifts / overrides 接口与排班页面；处理"请假当天还有预约""缩短班次会不会撞单"。
metadata:
  version: '1.0.0'
  spec: project-design/superpowers/specs/2026-09-11-nail-salon-booking-design.md
  sections: §4.3 / §6.4 / §9.3 / §5.2
---

# 排班

## 数据

| 表                            | 说明                                                                                                                     |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `biz_staff_weekly_shift`      | 周模板：`staff_id + store_id(可空=通用) + weekday(1=周一..7=周日) + start_time + end_time`；一天多段即多行；**物理删表** |
| `biz_staff_schedule_override` | 日期例外：`date + store_id(可空=通用) + type(off/custom) + start_time/end_time + reason`；**物理删表**                   |

校验：`start_time < end_time`；同一天同一美甲师同一个门店层的多个 `custom` 段**不重叠**；
`type='off'` 时 `start_time`/`end_time` **必须为空**（防"既请假又上班"的脏数据）。

## 求值优先级（可约时段用它取班次）

```
当天有 type='off'      → 当天不可约（忽略周模板）
否则有 type='custom'   → 用这些 custom 段替代周模板
否则                   → 用周模板中该 weekday 的所有段
```

### 门店层叠（阶段 1.11）：专属优先、通用兜底

`store_id` **可空即通用层**；填了即该门店专属层。求值时按**层**取，而不是混合取用：

```
求值(美甲师, 某日, 门店 A) =
    若 A 有专属例外 → 用 A 的专属例外            （请假就全天不可约）
    否则若 A 有专属班次 → 用 A 的专属周模板
    否则 → 用通用层例外 / 通用周模板            （老数据全在通用层，无需回填）
```

- 「整体级联而非混合」：A 店配了请假，**不会**再去叠通用层的自定义时段，避免拼出运营没配过的班；
- 不传 `storeId` = 只看通用层（老调用方行为不变）；
- 冲突保护**按店范围**：专属模板只查该店预约，通用模板查全部；
- 前端排班页 `source`（`store`/`shared`）标出当前用的是哪一层，防「我在 A 店改的怎么全门店都变了」的误会。

## 接口

| 方法   | 路径                                    | 说明                                                                                                   |
| ------ | --------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| GET    | `/biz/staffs/:id/weekly-shifts`         | 周模板，`?storeId=` 取该店实际生效层，返回 `{shifts, source}`                                          |
| PUT    | `/biz/staffs/:id/weekly-shifts`         | **整体替换**，`?storeId=` 决定替换哪一层（空=通用层）                                                  |
| GET    | `/biz/staffs/:id/overrides`             | 例外列表，支持 `from` / `to` / `storeId`                                                               |
| POST   | `/biz/staffs/:id/overrides`             | 新增例外（body 可带 `storeId`）；撞既有预约 409 + 清单，`force=true` 才落库                            |
| DELETE | `/biz/staffs/:id/overrides/:overrideId` | 删除例外                                                                                               |
| GET    | `/biz/staffs/schedule-calendar`         | **区间排班（日历用）**：`?from&to&staffIds&storeId` → `{from,to,staffs,cells}`；单次最多 62 天 / 50 人 |

> `schedule-calendar` 是给日历面板的**只读批量查询**：一次把「一批人 × 一段日期」的
> 班次全拿回来（含来源层 `source`），避免前端按人发起 N 次 `weekly-shifts` +
> `overrides`。它只做聚合展示，**不参与求值** —— 可约时段的真相仍在
> `SlotsService`（走周模板 ⇒ 例外 ⇒ 缓冲的完整优先级）。

> 用 **PUT 整体替换**而不是逐条 CRUD：UI 是一次编辑 7 天的表格，整体替换天然保证"同天各段不重叠"，
> 也免去前端做复杂增量 diff。权限点 `biz:schedule:list` / `biz:schedule:update`。

## 冲突保护（§6.4，最容易漏的一条）

**已存在的预约是事实，不允许被静默作废。** 任何会让既有预约落在无效区间的主数据变更都必须显式告知：

| 变更                       | 判定                                        | 行为                                                                                                  |
| -------------------------- | ------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| 新增 `off` 请假            | 该日存在 `pending/confirmed/arrived` 的预约 | **默认 409 拒绝**，响应带受影响单据（`bookingNo` / 时段 / 顾客）；`force=true` 才放行（前端二次确认） |
| `custom` 缩短 / 改时段     | 既有预约超出新时段                          | 同上                                                                                                  |
| 缩短 / 删除周模板班次      | 未来 30 天内既有预约超出新班次              | 保存前返回冲突清单，要求先改期                                                                        |
| 停用美甲师                 | 存在未完成预约                              | 拒绝                                                                                                  |
| 停用 / 删除服务项目        | 存在引用它的未完成预约                      | 拒绝（已完成单不受影响，快照已落库）                                                                  |
| 修改项目时长 / 缓冲 / 价格 | —                                           | 不动历史，只影响新单                                                                                  |

实现要点：冲突查询用 `shopDayRange(date)` 得到本地日区间，再查该美甲师该日
`status IN ('pending','confirmed','arrived') AND deleted_at IS NULL` 的预约，
用 §5.3 的对称 `gap` 判据比较 `[新段]` 与 `[已有预约 + gap]`。

## 验收（§12 B1）

- 「周一至周五 10:00–12:00 / 13:00–20:00」→ 周末可约时段返回空
- `off` 例外 → 该日返回空；`custom` 例外 → 改为自定义时段
- **该日已有预约时添加 `off` → 409 + 受影响清单；`force=true` 才落库**
- 缩短周模板导致既有预约越界 → 保存被拒并提示先改期

## 常见坑

- 把例外当成"叠加"（在周模板上再加一段）→ 实际是**替代**。
- `off` 记录里留了 `start_time`/`end_time` → 数据语义混乱，校验阶段就要拒绝。
- 整体替换时忘记先删后插在同一事务 → 中途失败会丢排班。
- 加 `off` 不检查既有预约 → 到店才发现撞单（v1.0 的最大业务漏洞，务必别回退）。
