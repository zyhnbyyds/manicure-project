# 收单退款冲退完成通知接口 - C# 示例

## 请求示例

```csharp
using System;
using System.Collections.Generic;
using Aop.Api.Util;

namespace SdkDemoTest
{
    public class AlipayTradeRefundDepositbackCompletedNotify
    {
        public static void HandleNotify(IDictionary<string, string> requestParams)
        {
            // 使用支付宝公钥验签
            string alipayPublicKey = "<-- 请填写您的支付宝公钥，例如：MIIBIjANBg... -->";
            bool signVerified = AlipaySignature.RSACheckV1(requestParams, alipayPublicKey, "UTF-8", "RSA2");

            if (signVerified)
            {
                // 验签通过，解析业务参数
                string bizContent = requestParams.ContainsKey("biz_content") ? requestParams["biz_content"] : "";
                // 可使用 JSON 库（如 Newtonsoft.Json）将 bizContent 反序列化为对象

                // 处理业务逻辑...

                // 返回 success 给支付宝，否则支付宝会重发通知
                Console.WriteLine("success");
            }
            else
            {
                Console.WriteLine("fail");
            }
        }
    }
}

```
