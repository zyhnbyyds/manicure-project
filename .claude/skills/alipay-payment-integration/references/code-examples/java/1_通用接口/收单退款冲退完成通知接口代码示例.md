# 收单退款冲退完成通知接口 - JAVA 示例

## 请求示例

```java
package com.java.sdk.demo;

import com.alipay.api.internal.util.AlipaySignature;
import java.util.HashMap;
import java.util.Map;

public class AlipayTradeRefundDepositbackCompletedNotify {

    public static void handleNotify(Map<String, String[]> requestParams) throws Exception {
        // 从 HTTP 请求中获取所有参数
        Map<String, String> params = new HashMap<>();
        for (String name : requestParams.keySet()) {
            String[] values = requestParams.get(name);
            String valueStr = String.join(",", values);
            params.put(name, valueStr);
        }

        // 使用支付宝公钥验签
        String alipayPublicKey = "<-- 请填写您的支付宝公钥，例如：MIIBIjANBg... -->";
        boolean signVerified = AlipaySignature.rsaCheckV1(params, alipayPublicKey, "UTF-8", "RSA2");

        if (signVerified) {
            // 验签通过，解析业务参数
            String bizContent = params.get("biz_content");
            // 可使用 JSON 库（如 Gson、Jackson）将 bizContent 解析为对象

            // 处理业务逻辑...

            // 返回 success 给支付宝，否则支付宝会重发通知
            System.out.println("success");
        } else {
            System.out.println("fail");
        }
    }
}

```
