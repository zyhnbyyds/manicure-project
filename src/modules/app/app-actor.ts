/**
 * app 端（小程序）操作者的审计 id。
 *
 * 小程序域没有 `sys_user` —— 顾客不是后台用户，`created_by` / `updated_by` 也不是外键。
 * 统一记 `0` 表示「来自小程序端的顾客自助操作」，与后台操作者（真实 `sys_user.id`）
 * 区分开：事后审计时按 `created_by = 0` 就能一眼筛出「顾客自己干的」。
 *
 * 抽成一个共享常量而不是各处写 0：散落的魔法数字没人知道 0 是什么，
 * 也容易被误当成「某个 id 为 0 的用户」。
 */
export const APP_ACTOR_ID = 0;
