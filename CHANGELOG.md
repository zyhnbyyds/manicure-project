# 更新日志

美甲店到店预约与经营管理系统（后台管理端 + 微信小程序端）。

版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)，格式参考
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
面向门店人员的操作说明见 `docs/`，面向开发者的技术文档见 `dev-docs/`。

## 未发布

### 新增

- **Docker 一键部署**：`deploy/up.sh`（Linux / macOS）与 `deploy/up.ps1`（Windows）一条命令拉起
  后端、后台前端、MySQL 8.4、Redis 7。首次启动自动生成 JWT 密钥与管理员密码，自动建库、跑迁移、灌种子数据，
  数据落在 `mysql-data` / `redis-data` / `uploads` 三个命名卷里，`down` 不丢数据。
  含后端三阶段 `Dockerfile`、前端 `web/Dockerfile` + `web/nginx.conf`（静态托管 + `/api/` 反向代理）、
  `deploy/api-entrypoint.sh`（等库 → 迁移 → 按需种子 → 起服务）与 `deploy/env.example` 环境变量模板。
  详见 [Docker 一键部署](./dev-docs/quality/docker.md)。
- **文档站可选容器**：`docs` 服务把 `docs/`（门店操作手册）与 `dev-docs/`（开发者文档）两套
  VitePress 站合并构建成一个 nginx 静态镜像，挂 `profiles: ['docs']`，因此**默认不参与**
  `docker compose up`，与主栈零依赖关系。带 `--profile docs` 启用后同容器双路径访问
  （`/docs/`、`/dev-docs/`，端口 `DOCS_PORT` 默认 8080）。
  含 `deploy/docs.Dockerfile` 与 `deploy/docs.nginx.conf`。
  详见 [Docker 一键部署](./dev-docs/quality/docker.md) 第五节。
- **门店大屏展示**：新增 `/screen` 独立全屏页（**不挂后台布局** —— 没有侧边栏 / 顶栏 / 标签页），
  顶栏是品牌 + 门店/在岗人数 + 日期 + 大时钟，右侧留全屏 / 刷新 / 退出三个工具按钮。
  按美甲师分泳道铺开当日排期，时间块用**亮色实心圆角**，块内固定三行且**横向铺开**：
  第一行姓名与「到店 / 时长」分列两端、第二行「还剩多久」横贯整块、第三行时段居中 ——
  块宽就是时段长度，一个三小时的预约能占到八百多像素，三行文字全堆在左边会空出一大片纯色。
  第二行给全块最大的字号：「还剩 25 分 / 已超 8 分 / 还有 12 分」才是抬头看屏时真正要的答案，
  单位用「分」而不是 `′`（隔几米认不出那个符号）。底部进度条画「已用时间 / 总时长」。
  状态不写文字，全交给颜色 + 面板右上角的图例：待服务（靛蓝）/ 即将开始（琥珀，30 分钟内）/
  进行中（亮绿）/ 超时未完成（红）/ 已完成（灰）。
  **字号跟泳道行高走**（容器查询 `cqh`）：行数多则行高低、字自动缩小，三行永远塞得下；
  换 4K 屏行高翻倍，字跟着翻倍，不用改一行代码。块窄时另有一道**块宽上限**（`cqw`）兜底，
  并据此分四档砍信息：`lg` 全量、`md` 姓名/剩余/时段、`sm` 姓名与时段、`xs` 只保姓名 ——
  **砍的永远是次要信息，绝不为塞下内容把字号缩到看不见**。
  顾客首字头像与时长算「装饰件」，只在宽度 ≥ 25% 的块上才摆：
  两者合起来要一百多像素，实测会把 276px 的块里的姓名挤到只剩一个字（「微…」），
  而首字头像又和姓名首字完全重复（看起来就是「微 微…」）；
  它们的价值排序是 姓名 > 时长 > 头像，挤不下时从右往左砍。
  时间轴范围**只由有效预约决定**（排班只负责画、不参与定范围）并设 6 小时最小跨度：
  早先固定铺开 9:00~21:00，而门店模板排到 24:00，时间轴会被拉到 16 小时，
  一下午三单全挤成 109px 的窄条、字跟着一起小下去 —— 而这三单恰恰是这块屏上唯一要看的东西。
  顶部六项指标：今日预约 / 进行中 / 30 分钟内开始 / 待服务 / 已完成 / 在岗美甲师
  （超时单数不单开卡片，以红色小字挂在「进行中」下面，出现才占位置）。
  青色「当前时刻」竖线两端带箭头夹住此刻（青色是这套色板里唯一没被状态占用的色，
  用红会跟「超时」抢读数）、1 秒跳动的时钟、30 秒自动同步。
  泳道行**按剩余高度自动平分** —— 只有两三位美甲师时行高会被撑开铺满整屏；
  美甲师用昵称派生的**首字头像**（档案里没有头像字段），色相收在青→紫之间，
  避开会被误读成状态的红 / 琥珀 / 绿。
  配色为**固定的高对比深色**（自带 `--scr-*` 变量，**刻意不跟随后台可切换主题**），
  保证店里那块屏一眼可辨。已取消 / 未到店的预约**不占时间轴**（那段时间其实是空的），
  只在今日预约卡片下以「另有 N 单已取消」提示。入口在顶栏的 `MonitorPlay` 图标，
  新窗口打开，店内屏幕按 F11 全屏即为展示态。
- **顶栏新增「外部文档」入口**：一个图标点开下拉，可选「开发者文档」（`https://dev.fairtech.work`）
  与「商家文档」（`https://docs.fairtech.work`），都在新窗口打开。
  两个站点是分开部署的，所以收进一个下拉 —— 并排摆两个图标，等于为两个「一个月点一次」
  的入口各占一个常驻位。下拉项走 `window.open`：项是数据渲染出来的、挂不上真链接，
  但它由用户点击触发（有用户激活），不会被弹窗拦截器拦掉。

### 修复

- **文档站页脚的“最后更新”显示英文**：VitePress 2 已废弃扁平的 `themeConfig.lastUpdatedText`，
  旧写法**不报错、静默忽略**，导致页脚一直显示 `Last updated: Sep 14, 2026, 10:26 AM`。
  改用对象形式 `lastUpdated: { text, formatOptions }`，并用 `forceLocale: true` 让时间格式
  跟随站点 `lang`（现为“最后更新: 2026年9月15日 15:11”）。`docs/` 与 `dev-docs/` 两站均已修正。
- **AI 助手输入框样式没生效**：`ChatInput.vue` 里覆盖 lew-ui 输入框样式的选择器挂在了
  `LewTextarea` 的透传类名上，而 scoped 的 `:deep(x)` 编译成 `.祖先[data-v-xxx] x`（后代关系），
  该元素恰好就是 `x` 本身 → **永远匹配不到，且不报错**。症状：输入框仍是 lew-ui 的
  `#ebebef` 灰块 + 8px 圆角，聚焦时再叠一层白底与蓝色描边，与外层主题光圈形成双层边框。
  改把选择器挂到外层 `.ai-composer` 后生效；顺带修正高度 —— `LewTextarea` 没有 `rows`，
  写 `:rows="2"` 只会把它透传到根 `div` 上（对 div 无效），高度应由 lew-ui 的 `height` 决定。
- **AI 助手图标语汇统一**：会话列表每行去掉重复的机器人头像（一列全是同一个图标没有区分度），
  选中态改用整行底色区分；顶栏入口、面板工具条、会话列表与对话区标题统一为四角星芒
  （小尺寸用 lucide `Sparkles`，欢迎页主视觉用自绘 `AiStarIcon` 渐变），
  不再使用机器人头 `Bot`；也不用五角星 —— 五角星在本项目语汇里表示收藏 / 评分。

## 1.0.0 — 2026-09-15

首个正式版本。数据库 61 张表，实施批次 B1~B6 全部交付。

### 预约与排班

- **可约时段算法**：班次 − 已有预约 − 缓冲（对称 `gap`），含提前期（后台 0 分钟 / 小程序 60 分钟）、
  美甲师可做项目校验，并返回不可约原因（`off` / `no_shift` / `staff_cannot_do` / `fully_booked` / `out_of_window`）。
- **预约主链路**：创建（建单 + 收定/全款同一事务）、改期 / 改美甲师 / 改项目、确认、到店、完成、
  爽约、取消、结算尾款。冲突检测走 `SELECT ... FOR UPDATE` + 事务内复检，避免撞单与超订。
- **服务状态机与资金状态机**分离：服务状态与 `pay_status` 各自流转，派生字段（结束时间、应收、已收）可重算。
- **排班**：周模板**整体替换**（PUT，天然保证同天各段不重叠）、日期例外（请假 `off` / 自定义 `custom`），
  求值优先级为「日期例外 → 周模板」且**例外是替代不是叠加**。
- **主数据变更保护**：缩短或删除班次若让既有预约越界，保存前返回 **409 + 受影响清单**，`force=true` 才落库。
- **按门店排班**：`biz_staff_weekly_shift` / `biz_staff_schedule_override` 支持可空 `store_id`，
  `NULL` 即通用层；求值「专属优先、通用兜底」，整体级联（A 店请假不叠通用 custom）。
- **日历面板**：后台排班页与预约页新增日历视图（周视图 / 日视图共存），
  对应 `GET /biz/staffs/schedule-calendar` 与 `GET /biz/bookings/calendar` 两个只读区间接口。

### 会员与算价

- **等级折扣**以千分比存储，算价顺序固定为「等级折扣 → 积分抵扣 → 改价」。
- **储值**本金与赠送余额分离记账；充值走充值方案或自定义金额，赠送比例有上限校验。
- **次卡**：卡种（含适用项目）、发卡（储值支付时同事务条件扣款）、核销与到期。
- **积分**：累计、抵扣试算（服务端按上限复算）、兑换（同事务扣积分 + 发次卡）。

### 收银与资金

- **在线支付**：微信 Native 扫码、支付宝当面付；线下收款（现金 / 收款码）直接记账。
- **混合支付**：定金与尾款、多笔组合，实收口径统一为 `paid_amount = Σ received_amount`，
  次卡记 0、现金记实收、其余记应收。
- **回调安全**：验签 + 幂等，重复通知不重复入账；支持主动查单与超时关单。
- **退款按服务阶段分流**：服务开始前**无理由全额退**（金额服务端锁定、判责强制为门店）；
  开始后需 `biz:refund:approve`，金额必填且不超过支付单剩余可退，并做渠道额度预留与失败重试。
- **渠道对账**：差异清单与标记处理。

### 挂账与应收

- 挂账主体支持顾客 / 公司 / 员工，带额度与账期。
- 挂账下单**不写支付单**（`pay_status=credit`）；销账走条件更新，**不得超额**。
- 账龄分档与逾期标记；口径上**挂账不计营收，销账才计入**。

### 运营与报表

- **报表口径**：净营收 = 成功支付 − 成功退款；营业日按店内时区切分；次卡核销单列；支持 CSV 导出。
- **提成**：规则优先级 `服务项目 > 分类 > 美甲师`，计提基数默认实收（防挂账提前计提），
  支持补提与冲销（只改状态不删记录）、按期间结算冻结（结算后不可修改，只能冲销）。
- **评价**：一单一评、回复、隐藏、后台代录。
- **周期预约**：规则 + 滚动窗口批量生成，幂等游标保证不重复生成，改规则不回溯已生成单据。
- **通知**：短信 + 站内消息，模板变量声明与校验；事务提交后发送，发送失败**不回滚**业务，
  带失败重试与站内 inbox；未配置服务商时降级。

### 多门店（连锁直营）

- 单据带门店；门店上下文三级来源：显式 `storeId` → `x-store-id` 头 → 默认门店。
- 账号带门店可见范围（店长自动收窄）；美甲师可服务门店（空集合 = 全部）。
- 报表按店：**发生额按店，存量（会员资产）按全店口径**，边界在文档中显式说明。

### AI 助手

- 工具只调各业务 service、不直连数据库，权限点与后台接口一一对应，**不放大权限**。
- **只读工具**覆盖后台基座与美甲业务（预约 / 排班 / 基础数据 / 会员 / 收银退款 / 报表），
  全部 `L0` + 无需审批；**没有任何美甲业务写工具** —— 改单、收款、退款仍必须走对应页面。
- 风险分级（L0~L3）与审批流、操作预览、确认令牌、会话与任务留痕、支持撤销的工具带改动前快照。

### 微信小程序（B6）

- 独立认证域 `/api/v1/app/**` 与 `app_` 身份表，与后台 token **双向拒绝**；app 域不接后台 RBAC。
- 自助下单 / 取消、我的预约（含已退金额展示）、美甲师工作台、门店与服务项目目录、评价。
- 已知边界：**小程序内 JSAPI 支付仍是 501 契约位**，其余 app 域写接口均已实现。

### 后台管理端

- 46 个页面：业务 26 + 后台基座 18 + AI 面板 2。
- 列表统一 `useTable` + `LewTable` + `LewPagination`；弹窗统一 `LewModal` + `LewForm`（`formKey` 重建 + `setForm` 回填）。
- 菜单与权限由后端 seed 驱动，前端路由自动生成。

### 修复

- **分页上限**：单页硬上限统一为 `MAX_PAGE_SIZE = 200`（`src/modules/biz/common/query.ts`）。
  此前为 100，而列表接口不返回 `total`、前端靠「多取一条」判断下一页 ——
  每页选 100 时后端夹回正好 100 条，「下一页」按钮永久消失，第 101 条之后翻不到；
  走 zod `max(100)` 的接口更直接返回 400。各 controller 的 `pageSize` schema 现在统一引用该常量。
- **提成期间汇总**：改由服务端聚合（`GET /biz/commission-records/summary`）。
  原实现在前端拉 200 条再求和，某期记录超过 200 条会**静默少算**，
  而该数字用于结算前二次确认，会直接影响结算决策。
- **退款**：部分退款后的金额与进度在 C 端可见（补 `refundAmount`）。
- 通知模板下拉失败时不再把所有错误都当成「无权限」，仅对 403 降级。
- Modal 底部按钮统一改为 `footer-buttons`（lew-ui 的 `LewModal` 只有 `close` 事件，
  没有 `@ok` / `ok-button-props`）；危险操作统一走 `confirmDanger`（自带防重）。

### 工程与文档

- Bun 1.4 运行时；后端 NestJS 12 + Fastify + Drizzle ORM（MySQL）+ zod 4；前端 Vue 3 + lew-ui。
- 真实 MySQL 集成测试入口（`.env.test` + 独立库），单测与集成测试共 1332 例。
- 三层文档：`docs/`（门店人员操作手册）、`dev-docs/`（开发者技术文档）、
  `project-design/`（设计资料与踩坑记录），附链接与锚点自检脚本。
- 校验命令：`bun run typecheck` / `bun run lint` / `bun test`；前端 `cd web; bun run typecheck`；
  小程序 `cd miniapp; bun run typecheck`。
